function mult(val) {
    return val**2
}

function addFive(val) {
    return val + 5
}

function final(val) {
    return `the final result is ${val}`
}

module.exports = {
    mult,
    addFive, 
    final
}